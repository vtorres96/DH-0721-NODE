const dividir = require("./dividir")
const multiplicar = require("./multiplicar")
const somar = require("./somar")
const subtrair = require("./subtrair")

// Executando funcao de somar
// console.log(somar(10, 2));

// Executando funcao de subtrair
// console.log(subtrair(10, 5));

// Executando funcao de multiplicar
// console.log(multiplicar(3, 5));

// Executando funcao de multiplicar passando um parametro com valor 0
console.log(multiplicar(10, 0));

// Executando funcao de dividir
console.log(dividir(30, 5));

// Executando funcao de dividir passando um parametro com valor 0
console.log(dividir(10, 0));